//Reverse The String Without Built in method

let str="Piyush"
let reversed=""
for(i=str.length-1;i>=0;i--){
    reversed+=str[i]
}
console.log(reversed);

// The Loop started reverse using str.length-1
// Assigned the backward value to the reverse
// Print The Result